[module/pacman]
type = custom/script
exec = pacman -Q | wc -l
exec-if = command -v pacman
interval = 25

format =  <label>
format-foreground = ${colors.fg}

label = %output%
label-foreground = ${colors.fg}
