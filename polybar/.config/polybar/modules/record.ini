[module/record]
type = custom/script
interval = 1

exec = echo "Rec"
exec-if = test -f "/tmp/recordid"

format =  <label>
format-foreground = ${colors.red}

label = %output%
label-foreground = ${colors.fg}
